/**
 * 枚举类型定义文件
 */
enum LogColor {
  LOG = 'green',
}

enum ErrorType {
  FileNotFound = 'file is not found'
}

enum Postfix {
  JS = '.js',
  JSX = '.jsx'
}

export {
  LogColor,
  ErrorType,
  Postfix,
}